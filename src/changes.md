Changes that should be made to the codebase

1. Instead of using only StateSchema, it would be convinient to use Input and Output Schema as well. Currently StateSchema has to many parameter's who's purpose is only during input and output. They are not used by any of the nodes in the graph.

2. 
